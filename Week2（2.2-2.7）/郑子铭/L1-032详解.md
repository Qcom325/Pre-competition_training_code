1.用n和c分别表示目标长度和填充字符。
    //cin>>n>>c;
2.使用`cin.ignore()`跳过输入流中的换行符。
    //cin.ignore();
3.使用`getline(cin,s)`读取完整的原始字符串s
    //getline(cin,s);
    //如果不使用getline,而直接使用cin>>s,会导致空格截断问题。
4.根据s的长度和n的关系，输出结果分为两种情况：
    - 如果s的长度小于n,则在s的开头补充c字符，使其长度达到n。
    - 如果s的长度大于等于n,则截取s的最后n个字符。